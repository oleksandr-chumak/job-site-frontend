import React from 'react';
import {FormWrapper, LoginForm} from "@components";


const Login = () => {
    return (
        <FormWrapper>
            <LoginForm/>
        </FormWrapper>
    );
};

export default Login;